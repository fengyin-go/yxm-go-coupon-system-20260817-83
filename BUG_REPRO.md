# BUG_REPRO

## Bug 是什么

store 层把“记录不存在”和“记录冲突”包装成自定义 `StoreError`，但其 `Is` 方法把两类错误映射反了，导致 `errors.Is` 无法识别预期哨兵错误。

## 如何触发

1. 请求一个不存在的券/批次/用户券/使用记录 ID。
2. 或创建一个重名优惠券/批次/券码。

## 错误信息

接口返回 HTTP 500（默认错误），而不是预期的 404 或 409。
