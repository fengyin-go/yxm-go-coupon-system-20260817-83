# BUG_REPRO

## Bug 是什么

删除优惠券模板时没有检查是否仍有关联的发放批次或用户券，直接删除后留下孤儿数据。

## 如何触发

1. 创建优惠券模板和发放批次，并领取用户券。
2. 调用 `DELETE /api/coupons/{id}` 删除该优惠券。
3. 再尝试核销之前领取的用户券。

## 错误信息

删除接口返回成功，但之后核销用户券时报错（找不到关联优惠券）。
