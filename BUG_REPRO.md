# BUG_REPRO

## Bug 是什么

优惠券、批次、用户券、使用记录四类列表接口的分页起始索引按 0 基计算（正确应为 1 基），导致第一页跳过前面的数据、后续分页错位。

## 如何触发

1. 创建至少 3 条数据（优惠券、批次、用户券或使用记录均可）。
2. 请求 `GET /api/coupons?page=1&size=2`，其他列表接口同理。

## 错误信息

接口返回 `total=3`，但 `items` 长度只有 1；期望第一页应返回 2 条。
